#!/usr/bin/env node

const app = require('middleware').default;
const config = require('middleware/config').default;

const port = process.env.PORT || 8000;

app.listen(port, () => {
  config.logger.info(`Listening at PORT: ${port}`);
});
